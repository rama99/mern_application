const INITIAL_STATE = {

}

const bootcampReducer  = (state = INITIAL_STATE , action) => {
    switch(action.type) {
        default:
            return state;
    }
}


export default bootcampReducer;